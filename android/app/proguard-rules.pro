# Pure Dart/Flutter app with no reflection-based plugins; Flutter's default
# rules (proguard-android-optimize.txt) cover the engine. Add rules here if
# a future dependency needs to keep specific classes.
